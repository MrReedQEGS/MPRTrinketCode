The Reed++ language has code files with the extension .red
►word             
will print out that word and then start a new line

→word
will print out a word but not start a new line after the word is printed

►*varname 
will print out the value of the variable and put a new line

→*varname will print out a the value of a variable and not add a new line.

< is used for input statements

♣some commment    
comments start with ♣

#Type#variable#value  
to make an int variable #I#a#5  this is a=5 in python

∞start,end,[command!command!command]   
this is a for loop!!!!!
nested loops dont seem to work yet as when I split by comma I need to not inlcude the [] part!

#S#debug#ON
create a string variable called debug and set it to ON
to turn debug on to dump vars to the bottom of the prog
